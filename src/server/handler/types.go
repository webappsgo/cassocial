package handler

import "github.com/casapps/cassocial/src/server"

// Auth is an alias for server.Auth to avoid verbose qualification within this package.
type Auth = server.Auth

// Middleware is an alias for server.Middleware to avoid verbose qualification within this package.
type Middleware = server.Middleware
